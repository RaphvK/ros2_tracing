name: docker-ros

on:
  push:
    branches:
      - main
      - jazzy-ika
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  docker-ros:
    runs-on: ubuntu-latest
    steps:
      - uses: ika-rwth-aachen/docker-ros@main
        with:
          platform: amd64
          target: dev,run
          base-image: rwthika/ros2:jazzy
          rmw-implementation: rmw_zenoh_cpp
          command: ros2 launch tracetools_launch example.launch.py
          enable-recursive-vcs-import: false
          enable-industrial-ci: false
